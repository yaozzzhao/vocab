{
  "name": "vocabmaster",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --prefix frontend",
    "pages:dev": "npm run build --prefix frontend && wrangler pages dev frontend/dist --kv VOCAB_KV",
    "deploy": "npm run build --prefix frontend && wrangler pages deploy frontend/dist",
    "dev-frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "wrangler": "^4.90.0"
  }
}
